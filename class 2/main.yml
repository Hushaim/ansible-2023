- hosts: all
  tasks:
  - name: install apache
    yum:
      name: tree
      state: present 
    notify: Restart httpd
  
  - name: install apache
    yum:
      name: git
      state: present 
    notify: install wget 
     
  handlers: 
  - name: Restart httpd
    service:
      name: httpd
      state: restarted  

  - name: install wget 
    yum:
      name: wget
      state: present